# לינקים ישירים למערכות

המערכת תומכת בלינקים ישירים לכל אחת מהמערכות באמצעות URL parameters.

## 🔗 הלינקים

### מערכת הדרכות (Employee Certification)
```
https://your-domain.com/?system=training
```
או בפיתוח מקומי:
```
http://localhost:3000/?system=training
```

### מערכת ניהול ציוד (Equipment Calibration)
```
https://your-domain.com/?system=equipment
```
או בפיתוח מקומי:
```
http://localhost:3000/?system=equipment
```

### מסך בחירה (Landing Page)
```
https://your-domain.com/
```
או בפיתוח מקומי:
```
http://localhost:3000/
```

## 📝 הערות

- הלינקים יעבדו גם אם המשתמש לא מחובר
- במערכת ההדרכות, המשתמש יועבר אוטומטית למסך כניסה (שכעת דורש רק לחיצה על כפתור)
- במערכת הציוד, הגישה היא ישירה ללא צורך בהתחברות
- ניתן לשתף את הלינקים עם משתמשים אחרים

## 🚀 שימוש

1. העתק את הלינק המתאים
2. שלח אותו למשתמשים
3. המשתמשים יכנסו ישירות למערכת הרצויה

## 🔧 התאמה אישית

אם אתה רוצה לשנות את שמות הפרמטרים או להוסיף מערכות נוספות, ערוך את הקובץ `src/App.tsx` בשורות שמטפלות ב-URL parameters.
