# PostgreSQL Database Configuration (Supabase)
# ================================================

# Connection String מ-Supabase
# לקבלת ה-Connection String:
# 1. היכנס ל-https://supabase.com
# 2. בחר את הפרויקט שלך
# 3. לחץ על Settings -> Database
# 4. גלול למטה ל-"Connection string" -> "URI"
# 5. בחר "Connection pooling" (מומלץ)
# 6. העתק את ה-URI והדבק כאן (החלף [YOUR-PASSWORD] בסיסמה האמיתית)

DATABASE_URL=postgresql://postgres.[project-ref]:[YOUR-PASSWORD]@aws-0-[region].pooler.supabase.com:6543/postgres

# דוגמה:
# DATABASE_URL=postgresql://postgres.abcdefghijklmnop:MySecurePassword123@aws-0-us-east-1.pooler.supabase.com:6543/postgres

# Server Configuration
# ====================
PORT=5001
NODE_ENV=development

# הערות חשובות:
# ===============
# 1. אל תשתף את הקובץ .env עם אף אחד - הוא מכיל סיסמאות!
# 2. הוסף את .env ל-.gitignore
# 3. השתמש ב-Connection Pooling (פורט 6543) ולא ב-Direct Connection (פורט 5432)
# 4. אם אתה משתמש ב-Supabase, SSL מופעל אוטומטית
# 5. אחרי יצירת הפרויקט ב-Supabase, הרץ את schema.sql ב-SQL Editor

# שם קובץ זה ל-.env ושים אותו בתיקיית server/

